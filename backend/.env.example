# Database (default: SQLite for local dev; switch to MySQL for production)
# For MySQL: DATABASE_URL=mysql+pymysql://root:password@localhost:3306/campus_parking
DATABASE_URL=sqlite:///./campus_parking.db

# JWT
JWT_SECRET=change-this-to-a-random-string-in-production
JWT_EXPIRATION_MINUTES=480
